{
  "proxy": {
    "order": 50,
    "description": "configure proxy to use when connecting to Monaca Cloud",
    "longDescription": [
      "Configure a proxy server.",
      "",
      "The proxy server is defined as \"http://my.proxy.com:8080\"."
    ],
    "usage": [
      "monaca proxy"
    ],
    "examples": [
      "monaca proxy set http://my.proxy.com:8080",
      "monaca proxy rm"
    ]
  },

  "reconfigure": {
    "order": 70,
    "description": "generate default project configurations",
    "longDescription": [
      "Generate default project configurations and files.",
      "Running it without arguments will generate everything."
    ],
    "usage": [
      "monaca configurate"
    ],
    "options": [
      [
        "--transpile",
        "Generate transpile configuration files (both dev and prod)"
      ],
      [
        "--dependencies",
        "Install required build dependencies."
      ],
      [
        "--components",
        "Generate components folder."
      ]
    ],
    "examples": [
      "monaca reconfigure",
      "monaca reconfigure --transpile --components"
    ]
  }
}
